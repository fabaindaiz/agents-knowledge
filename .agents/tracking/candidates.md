# Candidates this repository offers

What this repository's harvest learned since the last release, in the form admission needs (`../knowledge/README.md`, *What a candidate carries*). The home repository reads these rows at the next release and empties the table; nothing here is followed as guidance.

| Candidate | Kind | Lacks | Evidence | First seen |
|---|---|---|---|---|
| extends test-double-fidelity — a check written for an input kind is tested on a real instance of that input, not only on a fixture of it: the fixture holds what its author thought of | K | a second occurrence | the bundle's home repository: a check for releases passed its fixture test and failed on every real release, because the fixture lacked one list a real release carries; found by an adversarial review, fixed in the same release | 2026-09-25 |
| extends same-answer-or-refuse — a reader of a documented subset of a standard format refuses every input the full parser would read differently, instead of reading it its own way | K | nothing | the bundle's home repository: a frontmatter reader kept typed scalars (booleans, dates, numbers) as strings where the standard parser types them; an equality check against the standard parser in CI found dozens of such inputs, and the reader now refuses them | 2026-09-25 |
| extends validate-each-transformation-run — a migration that turns hand-kept tables into generated ones is proven by regenerating the old tables in their old order from the new source and comparing them byte for byte, before the switch | K | a second occurrence | the bundle's home repository: several dozen index tables regenerated from note fields, identical to the previous release; the proof stays runnable against the release tag | 2026-09-25 |
| adversarial-review-per-attack-surface-before-a-tag — before tagging a release, independent reviewers each attack one surface (integrity, generation, loss, documents followed literally) and report only what they reproduced; fixes get at most two rounds | M | a second repository | the bundle's home repository: a release with every test green had a dozen blocking or silent defects found by three reviewers, among them a procedure no carrier could complete as written | 2026-09-25 |
| follow-the-procedure-literally-to-review-it — a document that tells an agent what to run is reviewed by running it, command by command, in a scratch copy | M | overlaps execute-the-procedure-to-review-it (queued) | the bundle's home repository: an update procedure, a bootstrap and a harvest each had one instruction that could not be followed as written; only a literal run showed it | 2026-09-25 |
