<!-- Generated by the release build from the full notes. Edit the source, never this file. -->
# Open — what the home is still waiting for

Generated at each release from the home repository's records, each cell shortened. A harvest reads it before offering anything: an experiment queued here is one this repository may be able to run, and a candidate listed here is extended with a new occurrence (`extends <slug>`), not offered again.

## Experiments waiting to be run

| Note | Experiment | Cost |
|---|---|---|
| nested-partial-update-replaces | for each store client in use, one test that emits an update from a nested payload and asserts its mask | minutes per client |
| absent-constraint-widens | seed two tenants, delete the scoping clause from one repository method and run the existing suite; record whether it stays green | minutes |
| a-default-scope-is-the-widest-one | for every command that writes, deletes or deploys over a set of targets, run it with no scope argument in its dry-run mode; record how many targets it would touch and whether its … | minutes per command |
| metric-against-trivial-predictor | compute each candidate metric for a constant predictor on a second dataset | minutes |
| close-the-loop-in-the-actuators-frame | sweep the lower hysteresis threshold in a probe; record mode changes and time-to-arrive per setting | minutes |
| cleanup-belongs-to-the-supervisor | on the unproven platform, run a probe that writes a marker into user storage; confirm the runner reports and removes it | minutes |
| derived-copy-goes-stale-silently | before each mtime-keyed step, replace a source with an older-dated edited copy; record which steps notice | minutes |
| abuser-controlled-exemption | for each cap in a control, construct the input that exceeds it and check whether the control still fires | minutes per control |
| coverage-measures-execution | run a mutation tool over one repository's suite and compare the mutation score with its coverage | an hour |
| a-check-must-be-seen-to-fail | for every check in a gate, record whether a planted violation was ever seen to turn it red; count the ones never seen | an hour per gate |
| copied-instruction-claims-its-origin | for each instruction file in a set of repositories, run every command it names and resolve every path; count the failing assertions per file, separating "never true here" from … | an hour per repository |
| sanitised-value-must-replace-the-raw | a lint rule that flags a sanitised binding whose source name is still read afterwards in the same scope, run across a repository; count how often the raw name survives | an hour |
| reproduce-the-checkout-not-only-the-environment | for every check in a gate, run it once from a clean export of the commit and once in the working tree; count the checks whose answers disagree | an hour per gate |
| one-write-gate-makes-read-only-real | run a suite against the real store in read-only mode with the store's access log enabled; count the writes that reached it anyway | an hour |
| untrusted-package-is-parsed-never-loaded | run the forged size constructions and the nine planted packages against a second, independently written reader; record peak allocation and each reason | an hour |
| read-a-snapshot-at-its-own-end | measure feature degradation at 1, 3, 7 and 14 days of refresh delay | an hour |
| derive-state-from-one-clock | run one time-driven piece both ways, accumulated deltas and derived from the clock, for its full length on the target; record the divergence at the end | an hour |
| validate-each-transformation-run | run the formatter unguarded over saved dirty trees in a scratch copy; count runs the guard would have rejected | an hour |
| unrunnable-system-moves-the-gate | build a startup that resolves the full dependency graph with stand-ins for hardware and credentials; count past startup failures it would have caught | hours |
| sweep-the-rendered-extremes | give a reviewer default-configuration screenshots of each recorded layout defect and count how many they flag | an hour |
| persist-inputs-derive-verdicts | preferences: in a store that saves every value, count users whose stored value equals the shipped default; change the default in a test build and count who is affected | an hour |
| absence-is-a-third-value | for each boolean in a schemaless store, count documents where it is missing and compare what the query layer and the application each conclude for them | an hour |
| kill-switch-reaches-every-path | for each switch, enumerate the feature's writers, readers and schedulers, flip it in a test and count the paths it does not stop | an hour per switch |
| in-process-guarantees | run two workers against the same resource in staging and fire concurrent requests; record double executions | an hour |
| retry-over-irreversible-effect | inject a lost response on the call between the two services and observe whether the downstream effect happens twice | an hour |
| test-double-fidelity | run each fake's test cases against the real dependency in an emulator and count the disagreements | an hour per fake |
| secrets-survive-rotation | rotate each secret in staging; count stored records that stop verifying and caches that keep serving | an hour per secret |
| censoring-is-information | train with unresolved rows labelled negative and with a horizon; compare calibration on the latest months | hours |
| detect-by-observation-not-build-flag | on the target device, show each detection signal in a readout and compare it with the device's real capabilities | one remote pass |
| absolute-level-is-a-time-index | rerun the adversarial check on a second dataset with a different growth curve; compare forward-period degradation with and without the transformation | hours |
| population-by-outcome | train on the filtered and on the full population; evaluate both on the unfiltered test block and report the ranking-metric difference | hours |
| order-writes-by-failure-residue | fault-inject a crash between each pair of writes in a test environment; classify each resulting state as recoverable or not | days |
| best-effort-side-channels | count a service's outbound calls, classify each as critical or best-effort, and measure how many propagate failure by default | days |
| fail-closed-defaults | count, in a fleet that *has* defaults, how often a service starts with a required variable unset | depends on logging |
| same-answer-or-refuse | removal half: for a month, count defects in the shared reader found by first-party runs versus by imports | a month of logging |
| no-simultaneous-deploy | for one pair with independent release cycles, measure how long both versions are live during a rolling deploy and during a rollback | needs production traffic |
| persist-inputs-derive-verdicts | at a policy change, count records whose stored verdict disagrees with the recomputed one | needs production traffic |
| refuse-only-on-unassertable-evidence | for each refusing signal, count distinct subjects per value in production and whether any value is client-settable | needs production traffic |
| same-answer-or-refuse | replay a period through both paths and count decisions that cross a threshold | needs production traffic |
| count-both-sides-and-use-a-control-window | for each join used in an investigation, publish per-side key coverage by month next to the finding | a history study |
| report-coverage-before-findings | add a coverage line to every existing report; count those that, on first run, evaluated less than they claimed | a history study |
| reproduce-the-checkout-not-only-the-environment | record the interpreter path next to every number written into a document; count disagreements that turn out to be environmental | a history study |
| ratchet-in-a-pinned-environment | across repositories that adopted a rule against a backlog, count how many adopted it at zero and later disabled it, against how many ratcheted it and still have it on | a history study |
| truncate-the-world-to-test-as-of | over a year of feature additions, count leaks the truncation test caught against those caught in review | a history study |
| merge-by-shared-fact-not-shared-shape | over one repository's history, classify each duplication removed or kept as fact or shape, and count the regressions that followed each | a history study |
| derived-over-chosen-identifiers | count prefixes independently chosen across a real set of repositories and look for duplicates | a history study |
| unrunnable-system-moves-the-gate | classify a period of defects in a repository that cannot be booted locally as logic, contract or composition, and compare the distribution with one that can | a history study |

## Candidates waiting for what they lack

| Candidate | Kind | Lacks |
|---|---|---|
| one-rule-two-readers-agree-on-the-verdict-not-the-refusal — a second implementation of one rule set is held to the same verdicts and not to the same refusals, so it rejects by … | K | a generality statement, and it may be an occurrence of `same-answer-or-refuse` or of `one-path-for-every-source` rather … |
| rank-on-raw-probability-on-calibrated — a step-function calibrator ties scores; rank on the raw score | K | generality (ML-narrow); fits `metric-against-trivial-predictor` as a boundary |
| append-only-at-a-fixed-schema — nulls from a schema change are indistinguishable from real missing values | K | an incident with a number; overlaps `no-simultaneous-deploy` |
| split-by-time-neither-side-for-straddlers — a row created before the boundary and observed after it belongs to neither side | K | novelty (temporal splits are known); the "neither side" nuance could extend `population-by-outcome` |
| distinct-event-kinds-stay-distinct — events that share a table are not the same kind of event | K | generality statement; an incident with a number |
| batch-get-is-unordered — attach the key to each result, never zip by request order | K | likely already known to an agent |
| cache-proven-by-equality — verify a derived cache by frame equality with the uncached path | K | fits `same-answer-or-refuse` as an occurrence |
| numerator-needs-its-attempt-denominator — a rate peak can be the retry schedule | K | novelty (analytics craft) |
| effective-support-before-pruning — zero contribution means redundant or structurally empty, which need opposite decisions | K | generality (ML-narrow) |
| write-audit-publish-by-pointer — immutable runs, a read-back audit, pointer flipped last | K | an occurrence against a real store (known pattern) |
| shadow-mode-logs-the-counterfactual — a monitor phase logs the verdict it would have given, down the enforce path | K | a second occurrence |
| detect-only-control-is-its-queue — an alert-only control is as good as the owned queue that drains it | K | a number; literature is SRE ch. 6 |
| committed-secret-is-not-a-gitignore-fix — untracking a committed secret fixes neither exposure nor risk | K | overlaps `secrets-survive-rotation`; could extend it |
| parallel-reads-ordered-verdicts — run independent checks concurrently, replay verdicts in policy order | K | a measured latency number |
| derive-dependent-config — never configure a value derivable from others | K | a boundary is already known (derivation couples controls) |
| append-only-collides-with-erasure — decide erasure (TTL, crypto-shredding) when adopting append-only | K | an incident; it is a recorded open collision |
| obligations-accrue-across-versions — ask "did anything since the subject's version require X" | K | a second occurrence |
| changelog-order-is-checked — newest-first is audited, not remembered | M | level 2 so far; one repository has several entries out of order |
| suspect-the-harness-first — when a probe surprises you, the harness is a test double and the first suspect | M | overlaps `test-double-fidelity` and principle 14; could be one sentence there |
| privileged-call-inside-the-host-gesture — a host that grants a capability only during a user activation must receive the request from its own event dispatch; an application loop … | K | a measurement on the target runtime (the design is unverified there); generality beyond browsers |
| classify-client-storage-by-rebuild-cost — data kept on a client that may be evicted without notice is classed by whether the application can rebuild it; only what it cannot … | K | an eviction occurrence (the policy is still an open question there) |
| accept-only-what-can-win — when two sources can hold the same id, resolve by a version the content carries, send ties to whoever acted last, and refuse at write time what the … | K | an incident |
| filter-noise-at-parse-in-a-distribution-gap — drop sub-perceptual artefacts where the data is parsed, so every consumer agrees, with the threshold placed in an empty gap of the … | K | a generality statement; natural-breaks literature not yet consulted |
| probe-compressibility-before-effort — spend compression effort only where a cheap trial shows gain; already-coded media does not compress | K | novelty (close to textbook) |
| anchor-to-the-frame-that-does-not-move — a coordinate stored relative to a reference that normally moves predictably is invisible until something else drives that reference | K | a second domain |
| readout-per-silent-outcome — where the runtime cannot be observed, every silent outcome gets a readout with distinct states (not wired, refused, empty, ok), and each question for … | K | an answer from the target runtime; overlaps `fail-closed-defaults` (fail visible) |
| one-field-one-decision — a derived classification that drives two behaviours couples them, and changing its input for one silently changes the other | K | novelty (orthogonality is textbook) |
| gate-reads-untracked-derived-state — on a checkout that has been away, rebuild the caches the gate reads before trusting its colour | M | overlaps `a-check-must-be-seen-to-fail` and `derived-copy-goes-stale-silently`; would be one bullet under step 0's … |
| routine-tools-leave-the-tree-as-found — regenerate only when the source is newer; restore temporary writes in `finally` | M | a second repository; near the existing *Generated artefacts churn* bullet |
| document-language-is-its-readers — a document's language follows its reader, not the conversation | M | a second repository; sits against the pre-flight's default of the conversation's language |
| changelog-ships-field — each entry says whether it changes the delivered artefact, pointing at the release step rather than restating it | M | evidence the field caught anything; the restated action went stale in the one repository that uses it |
| execute-the-procedure-to-review-it — reading a ritual with the intent to run it, or an adversarial "what if", finds what re-reading does not | M | a sharper statement; the two occurrences have different forms |
| non-developer-guide-by-symptom — a guide for a non-developer is organised by symptom, with every command and message checked against the code | M | a second guide |
| parallel-agents-on-disjoint-files — split bulk work by disjoint file sets, then verify by the invariant | M | a second occurrence |
| no-modification-time-no-window — a source whose records carry no modification time cannot be extracted incrementally by a re-read window alone, and the extractor's own maintenance … | K | literature (CDC, change-data-capture); a second repository |
| identity-preserving-copy-is-a-no-op-mutation — a "copy" through a constructor that returns its argument (a tuple of a tuple) changes nothing, so the mutation survives for the … | M | a second occurrence; it would extend principle 18's formatter example |
| refusal-must-not-read-like-an-answer — a function given an input outside its domain returns a well-formed value (the hash of nothing, an empty count) instead of refusing, and the … | K | a second occurrence; overlaps `fail-closed-defaults` (fail visible) and could be one form of it |
| bumping-a-monotone-counter-is-not-idempotent — a command that raises a version must refuse a second run against the same predecessor, because the numbers it burns can never be … | M | **the second occurrence arrived; what it still lacks is a second *tool***, which is the same gap … |
| split-a-checked-file-and-the-check-goes-blind — a structural check names the files it reads, so splitting one of them into several removes coverage without turning anything red | M | a second occurrence; overlaps `a-check-must-be-seen-to-fail` and `derived-copy-goes-stale-silently` |
| `an-append-only-log-cannot-be-grandfathered-by-line` — the exemption-by-line-number that makes a new rule shippable against pre-existing violations is unavailable in any document … | M | a second occurrence. It is a boundary on the existing grandfathering advice, not a new rule, so it may belong as one … |
| `a-budgeted-root-file-needs-a-named-displacement-target` + `a-budget-at-its-ceiling-is-a-queue` — a root instruction file under a hard line budget cannot take a new rule without … | M | a second repository; and whether it is a form of `a-second-source-of-truth-diverges-in-silence` rather than a claim of … |
| `conformance-to-an-artefact-spec-is-swept-not-remembered` — adopting a method gives a repository a set of artefact specs, and a repository drifts from them **in the direction the … | M | a second repository, and a boundary on how much of the sweep belongs in the shared method versus in the carrier's own … |
| `edit-tools-for-unrebuildable-artefacts-default-to-reporting` — a tool that edits an artefact nobody will rebuild to verify defaults to printing what it would change and writes … | M | a second occurrence, and a boundary: for an artefact that IS cheaply rebuilt the default costs a step and buys nothing |
| `address-by-content-coordinates-not-by-artefact-id` — a reference that must survive the regeneration of what it points into is keyed on **content-derived coordinates** (language … | K | a second repository. It reads close to *natural keys over surrogate keys*, and the literature on that was not … |
| `a-contentless-index-makes-row-identity-a-contract` — an index built without a copy of the text identifies its rows by position in the base table, so that correspondence becomes a … | K | a second repository, and a check on whether this is specific to one engine's contentless mode or general to any … |
| `the-narrower-artefact-declares-its-absorber` — when several artefacts of different breadth can be installed together, each narrower one **declares which wider one supersedes … | K | a second occurrence. The boundary is untested: it assumes containment is total, and a *mostly* narrower artefact would … |
| `a-version-bump-spends-the-forward-compatibility-it-was-protecting` — a record format whose readers ignore unknown fields can gain fields forever without a version change; the … | K | a second repository. The mechanism is textbook (must-ignore semantics); what may be new is the accounting — treating … |
| `one-field-two-bands-puts-the-cut-in-the-data` — when a single numeric field carries two different semantics over disjoint ranges, the boundary between them travels **in the … | K | a second occurrence, and an argument for why two fields were not simply used instead |
| `a-decoder-that-degrades-to-plausible-output-needs-an-out-of-band-check` — a decoder given the wrong parameters may not fail: it can return well-formed, entirely wrong output. … | K | a second domain. Compression is the instance here; the claim is meant to cover any decoder with a shared side-channel … |
| `a-cache-key-names-its-invalidators` — a memo over an expensive verification encodes every input that could change the answer — including the **version of the code that produced … | K | a second repository. Close to textbook cache-key hygiene; what is worth keeping is the **code version** in the key … |
| `an-artefact-the-runtime-maps-cannot-be-compressed` — a payload the runtime memory-maps is stored uncompressed inside its container, so its size is paid in full and the only lever … | K | generality beyond one platform's executable format, and it may be too platform-specific to be a claim at all |
| **Documented default values drift from the code that defines them.** One audit corrected about a dozen drifted defaults in a configuration reference in a single pass — a number, a … | K | Plausible and well evidenced, but the remedy is the interesting part and it is unsettled: generate the reference, check … |
| **Class-level state written from a subclass is not shared with its peers.** A class-level setter wrote onto the calling subclass, so sibling components never saw the state and … | K | Language-specific in its current form. Either generalise it to "state whose scope is not where it is written" — which … |
| **A logging context must be owned by the operation observed, not by the component that opened it.** Clearing the logging context when one component finished dropped it from every … | K | One occurrence, and it may be a special case of a wider claim about lifetimes crossing component boundaries. Worth a … |
| **A guardrail should state its real status** — confirmed, partial or gap — and name its assumptions inline, rather than reading as an absolute | M | This is about how a *method artefact* is written, not about building software, so it belongs in `method/` if anywhere. … |
| `gate-sequence-stops-at-the-first-red-step` — a gate declared as an ordered list of steps runs none of the steps after the first one that is red on arrival, so its last step is … | K | whether it is its own note or a boundary of `a-check-must-be-seen-to-fail` (a check can be *un-run*, not only … |
| `the-enforcer-is-exempt-from-nothing` — a tool added to check a rule set is held to that rule set, or it becomes the largest hole in it | K / M | an occurrence outside a bundle adoption; the remedy (exclude the tool, or make it conform) is a repository choice and … |
| `a-number-is-only-a-measurement-after-the-last-edit` — a figure recorded in a report describes the state it was taken in, and every edit after it turns it back into a claim … | K / M | whether it belongs in `method/` (how a session reports) or in knowledge (how any benchmark is quoted); it may be a … |
| `fix-at-the-call-site-leaves-the-others` — a defect worked around where it was noticed stays at every other call site of the same interface; fix the contract, or count the sites … | K | a second occurrence outside one repository; overlaps `kill-switch-reaches-every-path` in shape (one predicate, every … |
| `unconsulted-parameter-is-still-a-contract` — an option no implementation reads is not a no-op: callers act on what they believe it does, and two implementations can disagree … | K | literature; and a boundary — when honouring the parameter is the right repair rather than deleting it |
| `tolerant-of-absent-is-not-tolerant-of-malformed` — a parser made lenient for missing fields still raises on a field of the wrong shape, and the two failures mean different … | K | it may be a boundary of `absence-is-a-third-value` rather than a note; literature |
| `configuration-read-at-import-is-an-environment-test` — a module that reads configuration at import makes every test that imports it a test of the environment | K | a second occurrence; literature (the twelve-factor method states the principle, not this consequence) |
| `citation-checked-for-existence-is-not-checked-for-relevance` — a reference check that verifies the cited id exists cannot verify that it says what the citing sentence claims | K / M | a second occurrence; it is the limit an existing check already documents about itself, which may make it a method rule … |
| `an-error-response-is-a-retry-instruction` — after the irreversible effect has happened, failing the request to report that the bookkeeping write failed converts a *possible* … | K | it may be the same claim as `order-writes-by-failure-residue` ("ack right after the one non-idempotent step") in … |
| `a-projection-turns-not-requested-into-absent` — a read that names its fields hands the rest back as missing, and code downstream cannot tell "not set" from "not fetched" | K | a second occurrence; overlaps `absence-is-a-third-value` strongly enough that it may be an extension |
| `a-shared-rule-has-one-enforcer-per-copy` — a rule that travels as shared text is enforced by a script that does not travel with it, so changing the rule leaves every copy … | K / M | a second occurrence outside a bundle distribution; it is the dual of `copied-instruction-claims-its-origin` and may … |
| extends `absence-is-a-third-value` — a third source of "absent" beyond not-set and empty, **not-projected**, and a fourth, a numeric zero read through a truthiness test | K | a release to apply it, after deciding `a-projection-turns-not-requested-into-absent` above, which may be the same … |
| extends `copied-instruction-claims-its-origin` — the same failure in **executable** code, not only in instruction files: a script copied as a template carries its assumptions … | K | a release to apply it |
| extends `a-check-must-be-seen-to-fail` — the discipline applied **backwards**, to a defect that already exists: before trusting that a checker would have caught it, revert the … | K | a release to apply it |
| extends `absent-constraint-widens` — two forms outside a data query: an **optional predicate parameter** whose default answers "not satisfied" widens what *executes* rather than … | K | a release to apply it |
| extends `ratchet-in-a-pinned-environment` — a count is code × environment × **the state of the working tree it was taken in**; and a rule introduced against a backlog needs its … | K | a release to apply it |
| extends `best-effort-side-channels` — the cost of the pattern, stated: a call inside a log-and-continue block is indistinguishable, from outside, between working and never having … | K | a release to apply it |
| extends `nested-partial-update-replaces` — the semantics of an embedded document store, measured rather than assumed | K | a release to apply it |

## Answered: admitted, folded, refused or discarded, not to offer again

`a-cap-that-hands-off-is-not-a-cap-that-exempts`, `a-constraint-needs-a-second-subject-to-be-visible`, `a-principle-can-have-no-subject-in-a-carrier`, `assert-the-reason-not-only-the-outcome`, `correction-lands-where-the-rule-is-enforced`, `default-scope-is-the-widest-one`, `fingerprint-fixes-its-collation`, `incoming-cannot-be-checked-empty-while-the-bundle-ships-a-file-into-it`, `parallel-session-id-allocation`, `skills-have-two-declared-homes-in-one-bundle`, `the-set-declares-a-version-the-changelog-does-not-reach`
